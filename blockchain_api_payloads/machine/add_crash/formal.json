{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title":"Add Crash Payload: POST /api/machine/<assetId>/crash",
  "description":"Definition of the payload for adding crash API request: POST /api/machine/<assetId>/crash",
  "properties":{
    "customerId":{
      "type":"string",
      "description":"Defines the username for authentication purposes."
    },
    "crash":{
      "type":"number",
      "description":"Defines the crash id."
    }
  },
  "required":[
    "customerId",
    "crash"
  ]
}
